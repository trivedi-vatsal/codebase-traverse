const { traverseDirectory } = require("./traverseDirectory");
const { searchFunctionInFile } = require("./searchFunctionInFile");
const { setFunctionArgs } = require("./setArgs");

module.exports = {
  traverseDirectory,
  searchFunctionInFile,
  setFunctionArgs,
};
